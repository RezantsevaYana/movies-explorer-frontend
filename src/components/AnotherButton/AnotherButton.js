import React from "react";

function AnotherButton(props) {
    return (
        <section className="another-button__container">
            <button className="another-button">Ещё</button>
        </section>
    );
}

export default AnotherButton;